# Alembic config. The DB URL is supplied at runtime from app.config (env-driven),
# never hardcoded here — so the same migrations run on SQLite / local Postgres / ODP.
[alembic]
script_location = %(here)s/alembic
prepend_sys_path = %(here)s
